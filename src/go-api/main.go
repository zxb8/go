package main

import (
	"go-api/api"
)

func main(){
	api.Run()
	//port := "3000"
	//fmt.Printf("Api running on port %s \n",port)
	//r := routes.NewRouter()
	//log.Fatal(http.ListenAndServe(fmt.Sprintf(":%s",port),r))
}
