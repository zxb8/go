syntax = "proto3";

package addservice;

service AddService{
    rpc Add(AddRequest) returns (AddResponse);
}

message AddRequest{
    double a = 1;
    double b =2;
}

message AddResponse{
    double result =1;
}